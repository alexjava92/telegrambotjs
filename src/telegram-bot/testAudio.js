/*import fs from 'fs';
import path from 'path';
import { generateAudioInText } from "../chat-gpt/chat-gpt.js";

// Путь к тестовому аудио файлу
const testAudioFile = path.join('D:', 'git', 'telegrambotjs', 'src', 'telegram-bot', '1713343691862.mp3');

// Функция для тестирования
async function testGenerateAudioInText() {
    try {
        // Проверяем, существует ли тестовый аудио файл
        if (!fs.existsSync(testAudioFile)) {
            console.error('Тестовый аудио файл не найден');
            return;
        }

        // Вызываем функцию generateAudioInText с тестовым аудио файлом
        const transcribedText = await generateAudioInText(testAudioFile);

        // Выводим результат в консоль
        console.log('Транскрибированный текст:', transcribedText);
    } catch (error) {
        console.error('Ошибка при тестировании:', error);
    }
}*/

// Вызываем функцию для тестирования
/*
testGenerateAudioInText();*/
